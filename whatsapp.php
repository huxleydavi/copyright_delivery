<?php if ($_SERVER['REQUEST_METHOD'] == 'POST') { $cartItems = json_decode($_POST['cart-items'], true); if (!is_array($cartItems) || empty($cartItems)) { echo "Error: Cart items are empty or invalid."; exit(); } $mensagem = ""; $mensagem .= ""; $mensagem .= ""; $subtotal = 0; foreach ($cartItems as $item) { $mensagem .= ""; $subtotal += $item['price'] * $item['quantity']; } $mensagem .= ""; $mensagem .= "*"; $mensagem .= "*"; $mensagem .= "*"; $mensagem .= ""; $mensagem .= ""; // Send message to WhatsApp $link = ""; //header echo "Pedido realizado com sucesso!"; } else { echo "Erro: Método de requisição inválido."; } ?>